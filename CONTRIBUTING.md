# Contributing

Catalog changes are made through pull requests. Every change must preserve stable product IDs, provide English and Turkish content, pass validation, and avoid mutable installation URLs.

Release metadata may be marked `announced` before verified artifacts exist. It may be marked `installable` only when all required platform artifacts, digests, signatures, and compatibility declarations are complete.
