# Security Policy

Do not report security vulnerabilities in public issues. Contact `security@archiorbitlabs.com` with the affected product, version, and reproduction details.

Catalog entries must never contain credentials, tokens, connection strings, private endpoints, or inline secrets. Installation artifacts must use immutable release URLs, SHA-256 digests, and detached signature references before their status can be changed to `installable`.
